const arr1 = [1, 2, 3, 4, 5];
const arr2 = [6, 7, 8, 9, 10];


//arr.push(value) and arr.pop(); do their default operation



//merging two arrays:-
//concat copies the second array in the first array
 console.log(arr1.concat(arr2));


 //arr.forEach(function)
 //forEach is used to iterate over each element of an array
 //it performs the passed function as the parameter on each element of the array